---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<header>
  <div class="header__container">
    <h1 class="header__title">{title}</h1>
    <p>{description}</p>
  </div>
</header>
